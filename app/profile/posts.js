import React, { Component } from 'react'
import { TouchableOpacity } from 'react-native'
import { Content, Text } from 'native-base'

export default class Posts extends Component {
    render() {
        return(
            <Content>
                <Text>from posts</Text>
            </Content>
        )
    }
}